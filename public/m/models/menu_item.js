define(['jquery',
    'underscore',
    'backbone',
    'marionette'], function ($, _, Backbone, Marionette) {
    'use strict';

    var menuItem = Backbone.Model.extend({
        defaults: {
        },
        initialize: function () {
        }
    });

    return menuItem;
});